_schema-version: 3.3.0
ID: incident-management2
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
  deploy_mode: html5-repo
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: incident-management2-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: incident-management2-auth
      - name: incident-management2-db
      - name: incident-management2-destination

  - name: incident-management2-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: incident-management2-db

  - name: incident-management2-app-deployer
    type: com.sap.application.content
    path: gen
    requires:
      - name: incident-management2-auth
      - name: srv-api
      - name: incident-management2-html5-repo-host
        parameters:
          content-target: true
          config:
            HTML5Runtime_enabled: true
    build-parameters:
      build-result: app/
      requires:
        - name: incidentmanagement2incidents
          artifacts:
            - incidents.zip
          target-path: app/

  - name: incidentmanagement2incidents
    type: html5
    path: app/incidents
    build-parameters:
      build-result: dist
      builder: custom
      commands:
        - npm ci
        - npm run build
      supported-platforms:
        []

  - name: incident-management2-destinations
    type: com.sap.application.content
    requires:
      - name: incident-management2-auth
        parameters:
          service-key:
            name: incident-management2-auth-key
      - name: incident-management2-html5-repo-host
        parameters:
          service-key:
            name: incident-management2-html5-repo-host-key
      - name: srv-api
      - name: incident-management2-destination
        parameters:
          content-target: true
    build-parameters:
      no-source: true
    parameters:
      content:
        instance:
          existing_destinations_policy: update
          destinations:
            - Name: incident-management2-html5-repository
              ServiceInstanceName: incident-management2-html5-repo-host
              ServiceKeyName: incident-management2-html5-repo-host-key
              sap.cloud.service: incidentmanagement2.service
            - Name: incident-management2-auth
              Authentication: OAuth2UserTokenExchange
              ServiceInstanceName: incident-management2-auth
              ServiceKeyName: incident-management2-auth-key
              sap.cloud.service: incidentmanagement2.service

resources:
  - name: incident-management2-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: incident-management2-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
        role-collections:
          - name: 'support (incident-management2 ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.support'
          - name: 'admin (incident-management2 ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.admin'
  - name: incident-management2-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: incident-management2-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
      config:
        HTML5Runtime_enabled: true
        init_data:
          instance:
            existing_destinations_policy: update
            destinations:
              - Name: srv-api
                URL: ~{srv-api/srv-url}
                Authentication: NoAuthentication
                Type: HTTP
                ProxyType: Internet
                HTML5.ForwardAuthToken: true
                HTML5.DynamicDestination: true
    requires:
      - name: srv-api

  - name: incident-management2-html5-repo-host
    type: org.cloudfoundry.managed-service
    parameters:
      service: html5-apps-repo
      service-plan: app-host
